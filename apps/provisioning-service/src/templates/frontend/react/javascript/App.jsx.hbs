const styles = {
  main: { display: "flex", alignItems: "center", justifyContent: "center", height: "100vh", fontFamily: "system-ui, sans-serif" },
  h1:   { fontSize: "2rem", fontWeight: 700, color: "#111" },
};

export default function App() {
  return (
    <main style={styles.main}>
      <h1 style={styles.h1}>Welcome to DuckOps</h1>
    </main>
  );
}
