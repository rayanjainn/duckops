---
- name: Deploy application to Kubernetes
  hosts: all
  connection: local
  gather_facts: false

  vars:
    project_name: "{{ project_name }}"
    k8s_namespace: "{{ k8s_namespace }}"
    database_url: "{{ database_url }}"
    registry: "k3d-duckops-registry:5111"
    app_port: 3000
    ingress_host: "{{ (project_name + '-' + (github_username | default('duckops')) + '-duckops.' + (platform_domain | default('raycode.tech'))) if (is_cloud | default(false) | bool) else (project_name + '.localhost') }}"

  tasks:
    - name: Ensure namespace exists
      shell: |
        kubectl get namespace {{ k8s_namespace }} 2>/dev/null || \
        kubectl create namespace {{ k8s_namespace }}
      changed_when: true

    - name: Create Image Pull Secret (ECR)
      shell: |
        kubectl create secret docker-registry regcred \
          --docker-server={{ ecr_registry }} \
          --docker-username=AWS \
          --docker-password="{{ ecr_token }}" \
          --namespace {{ k8s_namespace }} \
          --dry-run=client -o yaml | kubectl apply -f -
      when: is_cloud | default(false) | bool
      ignore_errors: true

    - name: Create ConfigMap for project
      shell: |
        kubectl create configmap {{ project_name }}-config \
          --from-literal=DATABASE_URL='{{ database_url }}' \
          --from-literal=NODE_ENV=production \
          --namespace {{ k8s_namespace }} \
          --dry-run=client -o yaml | kubectl apply -f -
      ignore_errors: true

    - name: Apply Kubernetes deployment manifest
      shell: |
        kubectl apply -f /tmp/duckops-projects/{{ project_name }}/k8s/deployment.yaml
      ignore_errors: true

    - name: Apply Kubernetes service manifest
      shell: |
        kubectl apply -f /tmp/duckops-projects/{{ project_name }}/k8s/service.yaml
      ignore_errors: true

    - name: Apply dynamic Ingress for project
      shell: |
        cat <<EOF | kubectl apply -f -
        apiVersion: networking.k8s.io/v1
        kind: Ingress
        metadata:
          name: {{ project_name }}
          namespace: {{ k8s_namespace }}
          annotations:
            traefik.ingress.kubernetes.io/router.entrypoints: web
        spec:
          rules:
          - host: "{{ ingress_host }}"
            http:
              paths:
              - path: /
                pathType: Prefix
                backend:
                  service:
                    name: {{ project_name }}
                    port:
                      number: 80
        EOF
      ignore_errors: true

    - name: Wait for deployment rollout
      shell: |
        kubectl rollout status deployment/{{ project_name }} \
          -n {{ k8s_namespace }} \
          --timeout=120s
      ignore_errors: true
