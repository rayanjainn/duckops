apiVersion: apps/v1
kind: Deployment
metadata:
  name: health-service
  namespace: duckops
  labels:
    app: health-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: health-service
  template:
    metadata:
      labels:
        app: health-service
    spec:
      containers:
        - name: health-service
          image: k3d-duckops-registry:5111/health-service:latest
          ports:
            - containerPort: 4004
          env:
            - name: PORT
              value: "4004"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: duckops-secrets
                  key: database-url
            - name: NODE_ENV
              value: production
          readinessProbe:
            httpGet:
              path: /health
              port: 4004
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "500m"
