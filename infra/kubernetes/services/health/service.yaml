apiVersion: v1
kind: Service
metadata:
  name: health-service
  namespace: duckops
spec:
  selector:
    app: health-service
  ports:
    - protocol: TCP
      port: 4004
      targetPort: 4004
  type: ClusterIP
