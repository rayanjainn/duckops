apiVersion: v1
kind: Service
metadata:
  name: pipeline-service
  namespace: duckops
spec:
  selector:
    app: pipeline-service
  ports:
    - protocol: TCP
      port: 4003
      targetPort: 4003
  type: ClusterIP
